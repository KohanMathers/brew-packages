import ./unix;
import ./utils;

function time() {
  return timestamp();
}

function fromUnix(time) {
  return unix.fromUnix(time);
}

function daysInYear(y) {
  return utils.daysInYear(y);
}

function isLeapYear(y) {
  return utils.daysInYear(y);
}
